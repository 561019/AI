# 当前框架可用功能清单

更新时间：2026-08-01

本文用于说明当前联调框架里“已经可以使用、已经接入但需继续验收、仅保留接口占位”的功能范围。判断依据来自当前仓库中的服务启动清单、模块能力登记表、模块验证平台和近期前端链路联调结果。

## 1. 当前总体状态

当前框架已经具备完整的本地联调骨架：

- 前端工作台可以通过应用层接口提交账号、对话、文件、知识库、意图确认和流程执行请求。
- L4 业务应用层、L2 业务引擎层、L1 基础模块层已经通过统一网关和能力码进行连接。
- 流程执行引擎可以接收意图分析结果，根据能力码调用数据操作、文档解析、规则计算、分析预测、内容产出、知识库问答、执行沙箱等模块。
- 调用审计可以按 Trace ID 查看模块之间的输入、输出、耗时、状态和错误。
- 模块验证平台可以按模块验证标准接口是否可达、契约是否合格、上游是否接入。

需要注意：当前是“联调框架可用”，不是所有业务模块都已经达到生产质量。部分模块仍是适配器或占位接口，真实业务准确性需要模块负责人继续完善。

## 2. 已验证可跑通的核心链路

### 2.1 前端到后端基础链路

可用功能：

- 账号创建、登录、会话保持。
- 项目、对话、消息的基础读写。
- 对话内上传文件并绑定账号、项目、对话。
- 前端意图确认卡展示、确认、重新识别。
- 知识库文件上传、列表展示、预览入口和账号级知识库展示。
- 后端页面 `platform.html` 查看模块验证、调用审计、流程逻辑图。

主要入口：

- 前端工作台：`http://localhost:5173/`
- 后端平台页面：`http://localhost:5173/platform.html`
- 应用层健康检查：`http://127.0.0.1:8100/health`

### 2.2 标准业务处理链路

可用链路：

业务应用层接口 → 业务引擎层接口 → 意图分析 → 本人确认 → 流程执行 → 对内通道 → 各业务引擎 → 基础模块层接口 → 返回流程执行 → 内容产出/用户结果 → 前端对话框

当前已经联通过的能力：

- 意图分析识别用户问题，并生成前端确认摘要。
- 流程执行引擎根据能力码编排任务、排依赖、调用模块。
- 文档表格解析引擎解析上传文件，输出正文、表格、字段、来源位置。
- 数据操作引擎读取、聚合、检索、持久化结构化数据。
- 分析预测引擎接收上游数据，输出预测结果。
- 规则计算引擎接收规则上下文和数据引用，输出规则校验结果、风险点、异常项。
- 内容产出引擎把模块结果整理为用户可读回答。
- 知识库问答引擎通过知识库检索结果形成回答。
- 执行沙箱可以通过流程执行链路被调用并返回执行结果。

## 3. L4/L2/L1 核心服务

### 3.1 网关与注册服务

| 服务 | 端口 | 当前状态 | 用途 |
| --- | --- | --- | --- |
| application | 8100 | 可用 | 前端统一入口，处理账号、项目、对话、文件、知识库、流程提交 |
| engine | 8200 | 可用 | L2 业务引擎层统一入口，转发意图、流程、内容、数据等请求 |
| foundation | 8300 | 可用 | L1 基础模块层统一入口 |
| registry | 8400 | 可用 | 能力登记、模块能力码查询、能力路由 |

### 3.2 启动与关闭

常用启动：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\framework\start_all.ps1
```

常用关闭：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\framework\stop_all.ps1
```

如果只看服务代码清单，当前框架登记了 37 个后端服务。

## 4. L2 业务引擎层可用功能

### 4.1 意图分析引擎

服务：`intent`，端口：`8000`

能力码：

- `intent.analyze`

当前可用功能：

- 识别用户自然语言问题。
- 识别是否关联当前对话上传文件。
- 生成用户确认摘要。
- 将用户问题映射到平台能力码和后续任务。

当前边界：

- 意图分析负责“理解用户要什么”，不直接办理业务。
- 不应输出过多模块执行细节给普通用户。
- 如果模型网关不可用，复杂意图质量会下降或失败。

### 4.2 流程执行引擎

服务：`workflow`，端口：`8020`

能力码：

- `workflow.execute`

当前可用功能：

- 接收已确认意图。
- 根据能力码选择模块。
- 排定任务先后依赖。
- 调用业务引擎和基础模块。
- 汇总模块回执、业务结果、错误和人工确认项。
- 将可展示结果返回前端对话。

当前边界：

- 流程执行负责“怎么做、谁来做、先后怎么排”。
- 不应绕过网关直接乱取数据。
- 所有业务数据读取应通过数据操作引擎申请。

### 4.3 文档表格解析引擎

服务：`document_table_parsing`，端口：`8036`

能力码：

- `document.parse`
- `document.table.extract`
- `document.package.build`

当前可用功能：

- 解析上传文档、Excel、表格类材料。
- 输出表格行、字段、原始来源位置。
- 支持同一文件只解析一次，后续复用解析结果的框架已建立。

当前边界：

- 只负责识别、提取、结构化。
- 不负责预算计算、价格判断、项目审批。

### 4.4 数据操作引擎

服务：`data_operation`，端口：`8031`

能力码：

- `data.collect`
- `data.consolidate`
- `data.search`
- `data.persist`
- `data.trace`
- `data.catalog`
- `data.read`
- `data.create`
- `data.update`
- `data.delete`
- `data.aggregate`

当前可用功能：

- 读取平台结构化业务数据。
- 对上传解析结果和平台数据做检索、聚合、统计。
- 记录数据来源、数据标签、调用链路。
- 向流程执行引擎返回结构化结果和证据引用。

当前边界：

- 数据操作引擎负责业务数据的逻辑操作。
- 物理读写和底层存储应下沉到 L1 数据基础模块。

### 4.5 分析预测引擎

服务：`analysis_prediction`，端口：`8030`

能力码：

- `analysis.financial_statement`
- `analysis.price_forecast`
- `analysis.business_metric`

当前可用功能：

- 接收流程执行引擎传入的上游数据。
- 进行业务指标分析、趋势预测、价格预测等。
- 已接入新的交付包和平台模型调用适配。

当前边界：

- 需要上游提供明确的分析目标、预测周期和可计算数据。
- 预测结果只作为经营参考，不替代人工审批。

### 4.6 规则计算引擎

服务：`rule`，端口：`8010`

能力码：

- `rule.calculate`

当前可用功能：

- 预算完整性检查。
- 盈亏平衡输入检查。
- 规则异常、风险点、人工核对项输出。

当前边界：

- 负责规则核算，不负责生成最终业务话术。
- 规则结果应交给流程执行引擎和内容产出引擎整合。

### 4.7 内容产出引擎

服务：`content`，端口：`8011`

能力码：

- `content.generate`

当前可用功能：

- 把流程结果、规则结果、预测结果、知识库结果整理成用户可读回答。
- 生成业务成果草案、摘要、说明、建议。

当前边界：

- 内容产出负责表达，不应伪造上游没有验证的数据结论。

### 4.8 知识库问答引擎

服务：`knowledge_qa`，端口：`8038`

能力码：

- `knowledge.query`
- `knowledge.qa.answer`
- `knowledge.qa.contextual_answer`

当前可用功能：

- 根据用户问题向 L1 知识库检索材料。
- 基于检索片段生成知识问答结果。

当前边界：

- 问答质量依赖知识库是否已经完成解析、切片、索引。
- 知识库文件应绑定账号，不绑定项目和对话。

### 4.9 项目管理引擎

服务：`project_management`，端口：`8033`

能力码包括：

- `project.register.simple`
- `project.register.major`
- `project.approval.result.record`
- `project.member.add`
- `project.member.remove`
- `project.member.query`
- `project.member.update`
- `project.query`
- `project.trace.query`
- `project.list.query`
- `project.closure.execute`
- `project.archive.catalog.query`
- `project.archive.authorization.record`
- `project.archive.authorized.query`
- `project.archive.authorization.query`
- `project.grade.change.request`
- `project.grade.change.result.record`
- `project.task.progress.record`
- `project.task.final.callback`
- `project.task.query`

当前状态：

- 已接入适配器。
- 可以参与项目登记、项目查询、审批记录、任务进度记录类联调。
- 真实项目管理业务规则仍需模块负责人验收。

### 4.10 监控提醒引擎

服务：`monitoring_reminder`，端口：`8034`

能力码包括：

- `monitor.item.register`
- `monitor.item.update`
- `monitor.item.enable`
- `monitor.item.pause`
- `monitor.item.resume`
- `monitor.item.disable`
- `reminder.handle`
- `reminder.confirm.record`
- `reminder.escalate.record`
- `reminder.recover.record`
- `monitor.trace.query`

当前状态：

- 已接入适配器。
- 可以参与监控项登记、提醒、待办、升级、恢复类流程联调。

### 4.11 数字资产引擎

服务：`digital_asset`，端口：`8032`

能力码包括：

- `asset.create`
- `asset.update`
- `asset.delete`
- `asset.query`
- `skill.model_evaluation.register`
- `skill.development.request`
- `skill.implementation.register`
- `knowledge_source.register`
- `knowledge_source.result.register`

当前状态：

- 已接入适配器。
- 当前主要用于智能体、技能、知识源等数字资产登记。
- 不等同于业务数据仓库；业务数据应走数据操作引擎和数据基础模块。

### 4.12 多媒体生成引擎

服务：`multimedia_generation`，端口：`8035`

能力码：

- `multimedia.generate`
- `multimedia.poster.plan`
- `multimedia.text_to_image`

当前状态：

- 已接入适配器。
- 可用于宣传文案、海报规划、文生图等生成类联调。

### 4.13 知识地图引擎

服务：`knowledge_map`，端口：`8039`

能力码：

- `knowledge_map.create`
- `knowledge_map.update`
- `knowledge_map.select_resource`
- `knowledge_map.query`

当前状态：

- 当前主要是占位/适配入口。
- 真实知识地图选择资源、组织知识关系的能力还需要模块交付。

### 4.14 外部系统对接引擎

服务：`external_system_integration`，端口：`8037`

能力码：

- `external.system.invoke`
- `external.api.call`
- `external.callback.handle`

当前状态：

- 当前是占位入口。
- 现阶段平台核心联调可以不依赖它。

## 5. L1 基础模块层可用功能

### 5.1 权限管理

服务：`permission`，端口：`8001`

能力码：

- `permissions.check`

当前可用功能：

- 检查当前账号是否可以调用某个能力。
- 为文件、数据、知识库和流程请求提供权限前置判断。

### 5.2 大模型调度

服务：`model`，端口：`8002`

能力码：

- `model.respond`

当前可用功能：

- 统一调用平台配置的大模型。
- 为意图分析、内容产出、分析预测等模块提供模型能力。

当前限制：

- 如果模型 Key、网络或模型服务异常，会出现“模型网关不可用”。
- 复杂业务问题不能在模型不可用时伪造可信结论。

### 5.3 流程模板管理

服务：`template`，端口：`8004`

能力码：

- `template.retrieve`
- `template.list`
- `template.validate`
- `template.register_draft`
- `template.update_draft`
- `template.publish`
- `template.disable`

当前可用功能：

- 查询、校验、登记、发布和禁用流程模板。

### 5.4 账号网关

服务：`account_gateway`，端口：`8050`

能力码包括：

- `account.identity.resolve`
- `account.identity.verify`
- `account.session.resolve`
- `account.session.close`
- `account.resource.query`
- `account.create`
- `account.list`
- `account.update`
- `account.delete`
- `account.freeze`
- `account.handover_confirm`
- `account.offboarding_assets.query`

当前可用功能：

- 账号创建、登录、身份解析、会话解析。
- 账号资源查询和生命周期操作。

### 5.5 安全合规

服务：`security_compliance`，端口：`8051`

能力码：

- `security.guardrail.check`
- `security.output.review`
- `security.action.precheck`

当前可用功能：

- 输入输出安全检查。
- 动作执行前合规检查。

### 5.6 人机协同

服务：`human_collaboration`，端口：`8052`

能力码：

- `human.task.create`
- `human.task.respond`
- `human.task.remind`
- `human.task.escalate`
- `human.task.query`

当前可用功能：

- 创建真人确认任务。
- 记录真人回复。
- 查询、提醒、升级待办。

### 5.7 执行沙箱

服务：`execution_sandbox`，端口：`8053`

能力码：

- `sandbox.run_task`
- `sandbox.run_code`
- `sandbox.run_browser`
- `sandbox.result.query`

当前可用功能：

- 流程执行引擎可以调用执行沙箱。
- 支持 Python 代码执行、任务执行、浏览器执行、结果查询。
- 已验证链路：前端等价请求 → 流程执行 → 基础网关 → 执行沙箱 8053 → 交付沙箱 8765 → 返回 stdout/exit_code/证据。

### 5.8 进化机制

服务：`evolution_mechanism`，端口：`8054`

能力码：

- `evolution.candidate.create`
- `evolution.risk_check`
- `evolution.confirm`
- `evolution.approve`
- `evolution.publish`
- `evolution.rollback`
- `evolution.audit.query`

当前状态：

- 已接入适配器。
- 可参与候选能力、风险检查、确认、审批、发布、回滚和审计类联调。

### 5.9 知识库基础模块

服务：`knowledge_base`，端口：`8055`

能力码：

- `knowledge.retrieve`
- `knowledge.material.get`
- `chunk.split`
- `vector.embed`
- `vector.index.upsert`
- `search.query`
- `search.rerank`
- `search.retrieve_context`

当前可用功能：

- 知识材料读取。
- 知识片段切分。
- 向量索引和检索入口。
- 为知识库问答引擎提供检索上下文。

当前要求：

- 知识库上传文件应由知识库链路完成解析、切片、索引。
- 每个知识库文件只需要成功解析一次，后续问答直接检索解析结果和片段。

### 5.10 上下文与提示词管理

服务：`context_prompt_management`，端口：`8059`

能力码：

- `context.intent.prepare`
- `context.capacity.evaluate`
- `context.handoff.generate`
- `context.handoff.import`
- `context.project.search`
- `context.account.search`
- `context.reference.import`

当前可用功能：

- 对话上下文准备。
- 传承材料生成与导入。
- 项目、账号、参考材料查询。

### 5.11 数据基础模块

服务：`foundation_data`，端口：`8060`

能力码：

- `foundation_data.read`
- `foundation_data.write`
- `foundation_data.query`
- `foundation_data.source.register`
- `foundation_data.catalog.list`
- `foundation_data.access.trace`

当前状态：

- 已有标准接口和占位适配。
- 负责物理层数据读写、数据源登记、数据目录和访问留痕。
- 后续大规模公司数据入库应以此模块为底层仓库，由数据操作引擎进行业务数据处理和调用。

### 5.12 驾驭机制

服务：`control_mechanism`，端口：`8061`

能力码：

- `control.policy.apply`
- `control.override.request`
- `control.session.monitor`

当前状态：

- 当前是占位/适配入口。
- 后续用于人工接管、控制策略、运行边界。

### 5.13 记忆管理

服务：`memory_management`，端口：`8062`

能力码：

- `memory.record`
- `memory.retrieve`
- `memory.candidate.decide`
- `memory.audit.query`

当前状态：

- 当前是占位/适配入口。
- 后续用于长期记忆、候选记忆决策和记忆审计。

### 5.14 设备与系统接口

服务：`device_system_interface`，端口：`8063`

能力码：

- `device.command.invoke`
- `system.interface.call`
- `system.event.receive`

当前状态：

- 当前是占位/适配入口。
- 后续用于设备调用、系统接口调用和事件接收。

### 5.15 成本管控

服务：`cost_control`，端口：`8064`

能力码：

- `cost.estimate`
- `cost.record`
- `cost.limit.check`
- `usage.meter.record`

当前状态：

- 当前是占位/适配入口。
- 后续用于模型调用、沙箱执行、生成任务等成本估算和用量记录。

## 6. 当前可以从前端验证的功能

### 6.1 普通业务问答

可测内容：

- 上传业务 Excel 或文档。
- 提问统计、预测、风险核对、摘要生成类问题。
- 查看意图确认卡。
- 确认后查看最终用户回答。
- 到 `platform.html` 的调用审计按 Trace ID 查看模块输入输出。

示例问题：

```text
请基于当前上传文件，预测桂中地区今年八月份需求，并计算盈亏平衡数量和预算风险。
```

预期链路：

意图分析 → 本人确认 → 流程执行 → 文档解析 → 数据操作 → 分析预测 → 规则计算 → 内容产出 → 前端回答

### 6.2 知识库问答

可测内容：

- 在知识库上传文件。
- 确认知识库列表显示该文件。
- 预览文件。
- 用该账号新建任意对话，询问知识库文件中的内容。
- 调用审计中应看到知识库检索和知识问答链路。

预期链路：

知识库上传 → 知识库基础模块解析/切片/索引 → 知识库问答引擎检索 → 内容整理 → 前端回答

### 6.3 执行沙箱

可测内容：

- 在前端对话中提出明确代码执行或沙箱任务。
- 确认后查看是否返回执行结果。
- 在调用审计按 Trace ID 查看是否经过 `workflow`、`foundation`、`execution_sandbox`。

示例问题：

```text
请通过执行沙箱运行一段 Python 代码，计算 1 到 100 的和，并返回执行结果。
```

预期结果：

- 前端应看到计算结果 `5050`。
- 调用审计应看到 `sandbox.run_code` 或 `sandbox.run_task`。

### 6.4 模块验证平台

可测内容：

- 打开 `platform.html`。
- 进入模块验证。
- 逐个执行模块验证用例。
- 查看通过、失败、上游未接入、契约不合格。

判定标准：

- `通过`：模块接口可达，返回符合平台契约。
- `失败`：模块返回错误、超时或输出不符合契约。
- `上游未接入`：接口入口存在，但真实交付模块或依赖服务未接入。
- `契约不合格`：模块有返回，但字段、状态码、输入输出结构不符合平台标准。

## 7. 当前仍需后续完善的点

以下内容不能算作完全生产可用：

- 所有模块的真实业务算法准确性。
- 知识地图、外部系统对接、驾驭机制、记忆管理、设备与系统接口、成本管控等占位模块。
- 大模型网关的稳定性和模型 Key 配置。
- 所有模块负责人交付包的正式合并、验收和契约修复。
- 大规模公司数据入库机制，需要继续完善“数据操作引擎处理业务数据，数据基础模块负责物理存储”的规范链路。
- 前端 UI 由前端负责人继续维护时，需要保护 `src/services` 和后端交互协议，避免覆盖接口逻辑。

## 8. 向模块负责人交付时应说明的接口要求

每个模块负责人至少需要保证：

- 模块代码放在对应模块目录。
- 模块启动后有健康检查。
- 支持平台标准 envelope 输入输出。
- 返回 `status`、`trace_id`、`request_id`、`data`、`error`。
- 出错时返回明确错误码和修复建议。
- 所有业务结论带证据来源。
- 不越权读取数据，统一通过上层或基础模块接口申请数据。
- 模块验证平台中对应验证用例必须通过。

## 9. 当前结论

当前框架已经可以支持平台联调、前端演示、模块接入验证和核心链路测试。已经能跑通的重点能力是：

- 前端工作台到后端网关。
- 账号、项目、对话、文件、知识库基础链路。
- 意图分析和本人确认。
- 流程执行引擎编排模块。
- 文档解析、数据操作、分析预测、规则计算、内容产出。
- 知识库检索问答链路。
- 执行沙箱链路。
- 调用审计和模块验证平台。

下一阶段重点不是继续堆固定场景，而是让各模块负责人按照统一契约接入真实模块，并通过模块验证平台逐项验收。
