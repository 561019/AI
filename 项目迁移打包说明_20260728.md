# 项目迁移打包说明

本迁移包用于把本地联调平台迁移到另一台 Windows 电脑。

## 包含内容

- `framework/`：后端框架、各层接口、模块代码、启动脚本、配置和数据。
- `framework/data/`：当前测试数据库、上传文件和入库数据。
- `Web/workbench/`：前端工作台源码。
- `contracts/`、`acceptance/`、`架构/`、项目说明文档等联调资料。

## 未包含内容

- `node_modules/`
- `Web/workbench/dist/`
- Python 缓存、测试缓存
- 旧的 zip 包和历史导入临时目录
- `.git/`

## 新电脑准备

1. 安装 Conda 或 Python。
2. 建议创建环境：

```powershell
conda create -n he python=3.11
conda activate he
```

3. 进入项目根目录安装后端依赖：

```powershell
pip install -r requirements.txt
```

如果没有统一依赖文件，请根据启动报错补装，例如：

```powershell
pip install starlette openpyxl
```

4. 安装 Node.js 后进入前端目录：

```powershell
cd Web\workbench
npm install
```

## 需要检查或修改的路径

解压到新电脑后，重点检查以下文件中是否存在旧路径：

- `framework/config/model.env`
- `framework/config/module.env`
- `framework/start_all.ps1`
- `framework/stop_all.ps1`
- `Web/workbench/src/services/platform-api.js`

如果出现 `C:\Users\21964\Documents\联调`，改成新电脑实际解压路径。

## 启动方式

后端：

```powershell
cd <新电脑项目根目录>
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\framework\start_all.ps1
```

前端：

```powershell
cd <新电脑项目根目录>\Web\workbench
npm run dev
```

## 关闭方式

```powershell
cd <新电脑项目根目录>
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\framework\stop_all.ps1
```

## 注意

- 如果迁移包包含 `framework/data/`，新电脑会保留当前测试账号、项目、对话、上传文件和入库数据。
- 如果不想保留历史测试数据，可在新电脑确认后再清理数据库。
- 如果模型 API Key 没有随包迁移，需要在 `framework/config/model.env` 重新配置。
