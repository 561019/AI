# 联调平台项目文件说明

> 本文用于帮助非开发人员识别联调目录中各文件和目录的职责。它按“运行时是否重要”说明项目结构，不逐行解释第三方依赖、自动生成缓存或每个 Python 的内部实现。
>
> 项目根目录：`C:\Users\21964\Documents\联调`

## 一、先认识项目结构

```text
前端工作台（Web/workbench）
        |
        v
L4 应用网关（8100）
        |
        v
L2 业务引擎（意图、流程、解析、规则、项目、知识库等）
        |
        v
L1 基础模块（账户、权限、数据、知识库、模型、记忆等）
        |
        v
统一数据库和文件对象存储（framework/data/foundation_data）
```

前端不应直接读取数据库，也不应直接调用 L1 或 L2 引擎。前端通过 L4 应用接口发起请求；L2 负责理解和编排业务；L1 负责账户、权限、数据、文件、知识库等基础能力。

## 二、根目录文件和目录

| 路径 | 作用 | 是否日常使用 |
|---|---|---|
| `Web/` | 当前正式使用的前端工作台源码。 | 是 |
| `framework/` | 后端服务、模块、数据库、运行脚本和后端页面。 | 是 |
| `contracts/` | 各模块之间约定的接口格式、错误码、OpenAPI 文档。 | 开发/排障时使用 |
| `acceptance/` | 自动化验收测试，验证接口、权限、异步任务和端到端链路。 | 测试时使用 |
| `架构/` | 架构图、存储设计、权限设计、阶段计划等说明资料。 | 查阅资料时使用 |
| `对话迁移包_AI平台框架_20260723.md` | 本项目此前联调过程和上下文的迁移说明。 | 查阅历史时使用 |
| `Web.zip` | 前端交付压缩包备份，不是当前运行入口。 | 备份 |
| `_incoming_web_20260723/` | 导入 Web.zip 后保留的来件副本，用于比较或回退参考。 | 历史参考 |
| `App.vue`、`_current_*.txt`、`_incoming_*.txt` | 前端融合时的比较辅助文件。真正运行的页面在 `Web/workbench/src/App.vue`。 | 历史/辅助 |
| `l1_2_template_management/` | 早期或独立的模板管理代码副本；正式运行模块位于 `framework/layers/foundation/template_management/`。 | 历史参考 |
| `联调文件-7.20-解压/` | 原始联调资料解压目录。 | 历史参考 |
| `node_modules/` | Node.js 前端依赖安装目录，由 npm 自动生成。不要手工修改。 | 自动生成 |
| `outputs/` | 工具或测试产生的输出目录。 | 自动生成 |
| `.git/` | Git 版本控制历史。不要手动修改或删除。 | 版本管理 |
| `.gitignore` | 告诉 Git 哪些数据库、缓存、密钥和构建文件不提交。 | 配置 |

## 三、前端：`Web/workbench/`

这是用户实际打开和操作的工作台。技术栈是 Vue 3 和 Vite。

| 路径 | 作用 |
|---|---|
| `package.json` | 前端项目清单：项目名称、Vue/Vite 依赖和命令。`npm run dev` 启动开发服务，`npm run build` 构建发布文件。 |
| `package-lock.json` | 锁定依赖精确版本，保证不同电脑安装结果一致。不要手工编辑。 |
| `vite.config.js` | Vite 开发服务器和构建配置；也可能包含前端访问后端时的代理配置。 |
| `index.html` | 前端页面的 HTML 入口，Vue 会挂载到此页面。 |
| `README.md` | 前端运行方式及页面能力说明。 |
| `platform.html` | 独立的平台控制台入口页。 |
| `src/main.js` | Vue 启动入口，负责创建应用并加载根组件和样式。 |
| `src/App.vue` | 工作台主页面：登录、账户状态、项目、对话、上传文件、意图确认、工作流结果等主要交互。 |
| `src/PlatformConsole.vue` | 平台控制台的 Vue 页面组件。 |
| `src/styles.css` | 工作台主页面样式。 |
| `src/platform-console.js`、`src/platform-console.css` | 平台控制台页面的交互逻辑和样式。 |
| `src/data/demo.js` | 前端演示数据或静态辅助数据。接入后端的真实数据不应只保存在这里。 |
| `src/services/auth-api.js` | 账户注册、登录、登录态恢复、退出等接口适配。 |
| `src/services/workspace-api.js` | 项目、对话、消息、文件、工作区数据等接口适配。 |
| `src/services/platform-api.js` | 工作流、意图、通用平台接口适配。 |
| `src/services/knowledge-governance-api.js` | 个人/项目/组织知识库治理操作的应用层接口适配。 |
| `src/services/agent-management-api.js` | Agent 和 Skill 的创建、调整、发布、停用等应用层接口适配。 |

前端构建后的文件会出现在 `Web/workbench/dist/`。该目录是构建产物，不应直接改；应修改 `src/` 后重新执行构建。

## 四、后端总控：`framework/`

### 4.1 启停和运行脚本

| 文件 | 作用 |
|---|---|
| `start_all.ps1` | 启动全部后端服务和网关。 |
| `stop_all.ps1` | 停止由框架启动的后端服务。 |
| `status_all.ps1` | 检查各服务是否在监听、端口和进程状态。 |
| `run_services.py` | 服务启动调度程序。 |
| `run_cluster.py` | 以一个父进程统一管理服务的启动方式，适合一次性测试或 CI。 |
| `server.py` | HTTP 服务基础实现，统一承载各模块的 HTTP Handler。 |
| `core.py` | 通用运行核心：请求处理、服务配置、公共运行逻辑。 |
| `http.py` | HTTP 请求与响应的通用工具。 |
| `.run/` | 服务运行时产生的 PID、状态或日志辅助文件。停止服务后可能仍保留旧记录。 |

### 4.2 三个网关和平台公共文件

| 文件 | 作用 |
|---|---|
| `application_gateway.py` | L4 应用网关逻辑。前端主要通过它进入平台。默认入口端口为 `8100`。 |
| `engine_gateway.py` | L2 业务引擎网关，负责按规则把调用转到业务引擎。默认入口端口为 `8200`。 |
| `foundation_gateway.py` | L1 基础模块网关，负责受控访问基础能力。默认入口端口为 `8300`。 |
| `capability_registry.py` | 能力注册中心，记录模块名称、能力名、接口地址等。默认入口端口为 `8400`。 |
| `module_catalog.py` | 模块目录的程序定义，登记模块的层级、服务名、端口和能力。 |
| `MODULE_CATALOG.md` | 人能阅读的模块清单，列出 14 个 L2 引擎和 15 个 L1 基础模块。 |
| `module_verification.py` | 后端“模块验证”页所用逻辑，用代表性请求验证各模块接口。 |
| `platform_overview.py` | 后端平台概览页所用的数据和页面逻辑。 |
| `envelope.py` | 平台标准请求/响应信封格式，统一携带请求编号、追踪编号、状态和数据。 |
| `adapter_proxy.py`、`adapter_specs.py` | 模块适配器及其规格定义，用于将平台标准接口转换为模块实现接口。 |
| `intent_adapter.py`、`rule_adapter.py`、`permission_adapter.py` | 意图、规则、权限模块的专用适配器。 |
| `workflow_service.py` | 流程编排的通用服务辅助逻辑。 |
| `model_dispatcher.py` | 大模型调用的调度辅助逻辑。 |
| `data_catalog.py` | 数据集目录和访问规则定义：哪些模块可以读写哪些数据。 |

### 4.3 后端页面：`framework/web/`

| 文件 | 页面用途 |
|---|---|
| `modules.html` | 模块验证页。用于逐个检查 29 个模块的接口请求、响应和调用链。地址：`http://127.0.0.1:8100/modules`。 |
| `monitor.html` | 调用审计和 Trace 监控页。用于查看一次操作的模块调用、输入、输出、状态和耗时。地址：`http://127.0.0.1:8100/monitor`。 |
| `uploads.html` | 上传文件和对象存储的查看/测试页面。 |
| `chat.html` | 后端提供的简化聊天测试页面。 |
| `cases.html` | 预设业务案例的测试页面。 |
| `demo.html` | 通用演示页面。 |

### 4.4 后端配置和说明：`framework/config/`

| 文件 | 作用 |
|---|---|
| `model.env` | 当前机器实际使用的大模型配置。可能包含密钥，不能提交或外传。 |
| `model.env.example` | 大模型配置模板，不含真实密钥。 |
| `module.env.example` | 外部模块地址、超时等配置模板。 |
| `README.md` | 配置项说明。 |

### 4.5 后端说明文档

| 文件 | 作用 |
|---|---|
| `README.md` | 后端最小平台框架说明和基本启动方式。 |
| `DATA_PERSISTENCE.md` | 数据实际存放位置、数据表、对象存储、权限校验和查询方式。 |
| `SERVICE_RUNTIME_GUIDE.md` | 服务启动、关闭、状态检查和排障说明。 |
| `ENGINE_INTEGRATION_GUIDE.md` | L2 引擎如何接入平台、如何遵循接口规范。 |

## 五、后端模块代码：`framework/layers/`

每个模块目录通常包含两个文件：

- `__init__.py`：告诉 Python 这是一个可导入模块包。
- `service.py`：该模块真正处理接口请求、调用下游、生成结果的主要代码。

`business_application/modules.py`、`business_engine/modules.py`、`foundation/modules.py` 是各层模块注册清单。它们决定服务名称、端口、接口入口和能力名称。

### 5.1 L4 业务应用层：`business_application/`

| 目录/文件 | 作用 |
|---|---|
| `application_gateway/service.py` | 面向前端的应用接口。负责接收账户、项目、对话、上传、确认执行等请求，再按规则调用 L2。 |
| `modules.py` | L4 应用层模块注册信息。 |

### 5.2 L2 业务引擎层：`business_engine/`

| 模块目录 | 业务职责 |
|---|---|
| `intent_analysis/` | 识别用户在对话中想做什么，生成待确认的执行意图。 |
| `workflow_execution/` | 编排整个业务流程；依次调用解析、数据、规则、人工协同、内容产出等模块。 |
| `content_production/` | 将已验证的业务结果整理成用户可读的文字结论。 |
| `document_table_parsing/` | 解析 Excel、CSV、TXT 等文件，提取工作表、行号、字段和值。 |
| `data_operation/` | 业务层的数据读写、查询、追踪入口；再受控调用 L1 数据模块。 |
| `rule_calculation/` | 执行金额、差额、条件等规则计算。 |
| `analysis_prediction/` | 财务、价格、经营指标等分析和预测。 |
| `monitoring_reminder/` | 监控指标、生成提醒或待办。 |
| `project_management/` | 创建、查询、更新项目及项目归属。 |
| `external_system_integration/` | 对接外部系统和处理外部回调。当前没有业务需要时可不纳入流程。 |
| `knowledge_qa/` | 基于知识库内容进行检索和问答。 |
| `digital_asset/` | 管理 Agent、Skill、知识来源等数字资产。 |
| `knowledge_map/` | 建立和查询实体、关系、知识图谱。 |
| `multimedia_generation/` | 图片、海报、多媒体生成任务。 |
| `engine_gateway/` | L2 层内部统一网关服务。 |
| `generic_module_adapter.py` | 暂未专门实现的模块使用的通用适配实现。 |

有些引擎目录下还有 `delivered_engine/service.py`：这是已交付的原始引擎实现或模拟实现。外层 `service.py` 是平台适配层，负责补齐统一输入、输出、数据归属、审计和下游调用。

### 5.3 L1 基础模块层：`foundation/`

| 模块目录 | 基础职责 |
|---|---|
| `account_gateway/` | 账户创建、登录、会话、账户资料。密码摘要和会话记录受保护。 |
| `permission/` | 权限检查，决定当前账户能否访问或操作数据。 |
| `foundation_data/` | 统一数据持久化和数据集访问控制，是数据库的受控入口。 |
| `knowledge_base/` | 知识库本体、资料、切分、索引、检索能力。 |
| `memory_management/` | 对话记忆和上下文记忆管理。 |
| `context_prompt_management/` | 提示词、上下文模板和上下文长度管理。 |
| `model_dispatcher/` | 选择和调用配置的大模型。 |
| `human_collaboration/` | 创建需要人工处理、确认或补充材料的待办。 |
| `template_management/` | 流程和业务模板管理。 |
| `capability_registry/` | L1 层能力注册服务。 |
| `security_compliance/` | 安全检查、脱敏、合规控制。 |
| `control_mechanism/` | 平台控制策略和治理机制。 |
| `cost_control/` | 调用成本、资源用量统计和控制。 |
| `execution_sandbox/` | 受控执行脚本或工具的隔离环境。 |
| `device_system_interface/` | 设备或内部系统接口连接。 |
| `evolution_mechanism/` | 能力演进、评估或升级相关记录。 |
| `foundation_gateway/` | L1 层内部统一网关服务。 |
| `generic_module_adapter.py` | 暂未专门实现模块使用的通用适配实现。 |

## 六、数据和上传文件：`framework/data/`

这是当前联调环境最重要的数据目录。正常业务数据不应由前端直接写入此处。

| 路径 | 内容 |
|---|---|
| `foundation_data/platform_data.db` | 当前统一 SQLite 数据库。账户、项目、对话、消息、文件元数据、解析结果、流程记录、审计记录等主要数据在这里。 |
| `foundation_data/platform_data.db-wal`、`-shm` | SQLite 正在运行时产生的日志/共享内存文件。服务运行中会变化，不能手工删除。 |
| `foundation_data/objects/uploads/` | 当前上传文件的物理对象存储目录。文件名通常是内部对象编号，不是用户原文件名。 |
| `foundation_data/objects/uploads/upload_index.json` | 上传对象索引辅助文件。 |
| `platform.db` | 旧版或历史数据库，保留作兼容/备份；当前统一数据源优先使用 `foundation_data/platform_data.db`。 |
| `platform.db-wal`、`-shm` | 旧版 SQLite 的运行辅助文件。 |
| `workflow_instances.json` | 历史或辅助的流程实例快照文件；正式审计仍应以统一数据库和 Trace 为准。 |

与数据直接相关的主要表/数据集包括：

| 数据集 | 保存内容 |
|---|---|
| `accounts` | 账户基本资料。 |
| `account_credentials` | 加盐密码摘要，不能把它当作明文密码查看。 |
| `account_sessions` | 登录会话。 |
| `projects` | 项目及其账户归属。 |
| `conversations` | 对话主记录及其项目、账户归属。 |
| `conversation_messages` | 用户消息、意图确认、执行结果和失败结果。 |
| `uploaded_files` | 上传文件的名称、类型、大小、账户/项目/对话归属和对象引用。 |
| `storage_objects` | 物理文件对象的 ID、路径、大小、哈希和扫描状态。 |
| `parse_jobs` | 文件解析任务和状态。 |
| `extracted_fields` | 从表格中提取的字段、工作表、行号和值。 |
| `workflow_instances`、`workflow_node_instances`、`workflow_events` | 流程实例、每个节点、每个事件的留痕。 |
| `human_tasks` | 需要人工确认或补料的待办。 |
| `reconciliation_results` | 对账等业务流程形成的用户结果和证据摘要。 |
| `data_access_decisions` | 每次数据访问被允许或拒绝的审计决策。 |

旧的 `framework/uploads/` 是历史上传目录。当前新上传文件应写到 `framework/data/foundation_data/objects/uploads/`，并在统一数据库中登记。

## 七、接口契约：`contracts/`

这里的文件定义“模块之间怎样说话”。开发人员改接口时必须优先检查这里，避免一个模块改变字段后导致下游模块无法理解。

| 文件类型/文件 | 作用 |
|---|---|
| `*.openapi.yaml` | 某个接口的正式请求、响应、字段和地址约定，例如意图、流程、规则、账户、模型网关。 |
| `common-envelope.openapi.yaml` | 所有接口共用的请求/响应外壳格式。 |
| `layer-gateways.openapi.yaml` | 三层网关的接口规范。 |
| `platform-api-conventions.md` | 平台接口命名、版本、追踪、错误处理的通用规则。 |
| `module-interface-template.md` | 新增模块时应使用的接口文档模板。 |
| `module-adapter-matrix-v0.1.md` | 平台模块与交付引擎/适配器的对应关系。 |
| `async-task-and-callback.md` | 异步任务和回调接口的约定。 |
| `error-codes.md` | 如 `APPLICATION_COMMAND_FAILED`、`WORKFLOW_EXECUTION_FAILED` 等错误码的含义。 |
| `end-to-end-examples.md` | 端到端调用示例。 |

## 八、测试：`acceptance/` 和 `framework/test_cases/`

| 路径 | 作用 |
|---|---|
| `acceptance/run_acceptance.py` | 自动化验收总入口。 |
| `acceptance/config.example.json` | 验收测试配置模板。 |
| `acceptance/lib/` | 验收测试共用的 HTTP、配置、请求信封工具。 |
| `acceptance/tests/` | 自动化测试用例：健康检查、权限、接口契约、幂等、异步、对话、端到端等。 |
| `acceptance/scenarios/` | 可复用业务场景输入，例如销售佣金场景。 |
| `framework/test_cases/case2_sales_reconciliation.*` | 合同、回款、发票对账的测试案例说明和输入数据。 |
| `framework/test_cases/upload_samples/` | 可在前端上传的 Excel 测试样本。 |
| `framework/test_cases/run_case2_sales_reconciliation.ps1` | 执行案例二对账流程的脚本。 |
| `framework/test_cases/validate_data_pipeline.py` | 用临时数据库验证数据写入、读取和链路规则。 |
| `framework/test_cases/validate_data_security.py` | 用临时数据库验证数据权限、租户隔离、脱敏等。 |

## 九、查看运行结果时该看哪里

| 你要确认的事情 | 查看位置 |
|---|---|
| 服务是否全部启动 | 在根目录运行 `framework/status_all.ps1`。 |
| 模块接口是否可用 | `http://127.0.0.1:8100/modules`。 |
| 一次前端操作经过了哪些模块 | `http://127.0.0.1:8100/monitor`，按 Trace ID 查询。 |
| 某条调用的输入、输出、耗时和错误 | 监控页展开该条调用审计。 |
| 账户、项目、对话、文件等是否已保存 | `framework/data/foundation_data/platform_data.db`，或通过后端数据查询接口。 |
| 文件物理内容在哪里 | `framework/data/foundation_data/objects/uploads/`。 |
| 文件属于谁、哪个项目、哪个对话 | `uploaded_files` 数据集中的 `owner_account_id`、`project_id`、`conversation_id`、`object_id`。 |

## 十、修改时的边界

1. 修改页面和交互：优先改 `Web/workbench/src/`。
2. 修改前端到后端的请求格式：改前端 `src/services/`，同时核对 `contracts/` 和 L4 应用网关。
3. 修改业务流程：改 `framework/layers/business_engine/` 中对应的 `service.py`。
4. 修改账户、权限、数据、知识库等底层能力：改 `framework/layers/foundation/` 中对应的 `service.py`。
5. 修改数据表或数据访问规则：先检查 `framework/data_catalog.py` 和 `foundation_data/service.py`，不要让前端绕过接口直接修改 SQLite。
6. 不要手工修改 `node_modules/`、`dist/`、`*.db-wal`、`*.db-shm`、`.git/` 或已上传文件的内部文件名。

## 十一、最短的运行判断方法

1. 后端服务启动后，打开前端工作台。
2. 新建账户、项目、对话并上传文件。
3. 在对话中确认执行。
4. 复制本次操作的 Trace ID。
5. 打开 `http://127.0.0.1:8100/monitor`，查看每条调用的源模块、目标模块、输入、输出、状态、耗时和数据归属标签。
6. 在统一数据库或后端数据页核对账户、项目、对话、文件、解析结果和最终结果是否带有同一组归属 ID。

这套判断方式能够确认：前端是否真正接入后端、数据是否真正写入、数据归属是否正确，以及哪一个模块或接口在失败。
