CASDOOR_URL=http://casdoor:8000
GATEWAY_PORT=8080
JWT_SECRET=replace-with-at-least-32-random-bytes
PERMISSION_MODE=remote
PERMISSION_URL=http://127.0.0.1:8001
PERMISSION_TIMEOUT_MS=2000
L1_INTERFACE_TARGET_SERVICE_SECRET=replace-with-a-long-random-secret
# Must match L1_INTERFACE_IDENTITY_CONTEXT_SECRET in the layer interface.
# In the current HMAC implementation this is the account gateway JWT secret.
