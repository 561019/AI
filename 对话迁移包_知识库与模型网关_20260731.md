# 对话迁移包：知识库链路与模型网关问题

更新时间：2026-07-31
项目根目录：`C:\Users\21964\Documents\联调`

## 一、项目位置

- 前端：`Web/workbench`
- 后端框架：`framework`
- 共享数据库：`framework/data/foundation_data/platform_data.db`
- 原始上传文件：`framework/data/foundation_data/objects/uploads`
- 后端服务脚本：`framework/start_all.ps1`、`framework/stop_all.ps1`
- 平台审计页面：`http://localhost:5173/platform.html`

当前不要在未确认的情况下清理数据库，也不要使用破坏性 Git 命令。工作区已有大量用户修改，应在现有修改基础上继续工作。

## 二、用户最终目标

1. 文件上传后只解析一次并保存解析结果。
2. 后续涉及知识库的问题直接检索已保存的解析片段，不重新解析原文件。
3. 个人知识库只绑定账户，不绑定 Project 或对话框。
4. 知识库问答必须通过知识库问答引擎返回文件片段和来源。
5. 所有模块之间保留真实接口调用、输入、输出、耗时和 Trace ID。
6. 不能为了跑通而绕过权限、数据操作引擎或既定层间架构。

## 三、知识库正确链路

### 3.1 首次上传

```text
前端上传
→ application-gateway
→ engine-gateway
→ digital-asset
→ data-operation.data.persist
→ foundation-data 保存原文件和元数据
→ 返回 source_data_ref
→ knowledge-base.vector.index.upsert
→ 读取原文件并解析
→ data-operation/foundation-data 写入 knowledge_chunks、knowledge_indexes
→ 文件状态 indexed
```

原文件和解析结果的职责：

- 原文件物理对象：`storage_objects`
- 上传文件元数据：`uploaded_files`
- 数字资产登记：`digital_assets`
- 知识来源登记：`knowledge_sources`
- 知识库解析片段：`knowledge_chunks`
- 知识库索引状态：`knowledge_indexes`
- 接口调用审计：`interface_calls`
- 数据权限审计：`data_access_decisions`
- 数据变更留痕：`data_record_events`

### 3.2 后续问答

```text
前端提问
→ application-gateway
→ intent-analysis
→ workflow-execution
→ knowledge-qa
→ foundation-gateway.knowledge.retrieve
→ foundation-data 查询 knowledge_chunks
→ knowledge-qa 组织回答和证据
→ 前端展示
```

后续问答不应再次调用：

- `document.table.extract`
- `vector.index.upsert`
- 原文件解析

## 四、已完成的代码修改

### 4.1 数字资产引擎

文件：`framework/layers/business_engine/digital_asset/service.py`

已完成：

- 数字资产引擎已登记在业务引擎层，端口 `8032`。
- 支持 `asset.*`、`knowledge_source.register` 等能力。
- 按账户、知识库和文件 `sha256` 查询已有索引。
- 已有完成索引时返回 `reused`，不再调用知识库解析。
- 接受 `HTTP 200` 和 `HTTP 202` 的成功响应。

### 4.2 知识库模块

文件：`framework/layers/foundation/knowledge_base/service.py`

已完成：

- 增加第二层幂等检查，防止补解析接口重复生成片段。
- 重复文件返回 `reused`。
- 批量上传时可以逐个返回文件状态。
- 检索时按 `owner_account_id`、`knowledge_base_id`、`asset_scope` 过滤。
- 接受 `HTTP 200` 和 `HTTP 202` 的成功响应。

### 4.3 应用层上传

文件：`framework/layers/business_application/application_gateway/service.py`

已完成：

- 知识来源 ID 使用文件内容哈希，不再只使用临时上传 ID。
- 重复上传同一文件可以指向同一知识来源。
- 前端上传项分别记录 `knowledge_index_state` 和 `knowledge_chunk_count`。

### 4.4 数据操作和知识库问答

文件：

- `framework/layers/business_engine/data_operation/service.py`
- `framework/layers/business_engine/knowledge_qa/service.py`

已完成：

- 接受下游返回的 `200/202 + status=success`。
- 知识库问答通过 `knowledge.retrieve` 查询 `knowledge_chunks`。
- 返回结果包含文件名、文件 ID、知识来源 ID和片段序号。

## 五、已经验证的知识库数据

当前账户：`account-local-mrx8mcib`

### 5.1 Excel

文件：`综合联调测试主数据.xlsx`

- 文件 ID：`upl_ed9f6bc6a94d47ec`
- SHA256：`92a4943766c866a3e8f632b7f09b2e4d647e3ff49beda3d1c58b3235701cd253`
- 知识来源：`ksrc_800ab8bdc4b54bce9266767d`
- 知识库：`pkb-1785461049410`
- 状态：`indexed`
- 解析片段：`132`
- 后续重复上传记录显示：`reused`

Excel 片段内容正常，例如能够看到：

```text
项目编号 | P-GZ-2026-001
区域 | 桂中
产品 | 丰力A型复合肥
```

### 5.2 PDF

文件：`平台研发实施计划_第一阶段_v0_1_20260708.pdf`

- 文件 ID：`upl_e57d077d47924f6a`
- SHA256：`9437fd5cfbd6331e97b2eb62121858a563d52ac8a084b14ec8cb8a1b5487151c`
- 知识来源：`ksrc_1ca7dcfed5df161fc315259e`
- 知识库：`pkb-account-local-mrx8mcib`
- 状态：`indexed`
- 解析片段：`109`
- 上传 Trace：`upload-ms8e64ku-s0j6n7`

调用链审计显示 PDF 上传已经完整经过数字资产、数据操作、基础网关和知识库模块，链路没有中断。

但是 PDF 片段内容是 `%PDF-1.7`、`/FlateDecode`、`stream` 等原始 PDF 二进制/交叉引用内容，不是正常正文。结论：这是 PDF 格式解析能力问题，不是链路问题。后续需要给知识库或文档解析模块接入真正的 PDF 文本提取器。

## 六、最新模型网关故障

最新用户问题：

```text
请根据我个人知识库中的《综合联调测试主数据.xlsx》，告诉我项目编号、区域和产品名称。
```

最新 Trace：

```text
trace-ms8fh5wy-k1obx3
```

任务：`ee72a69e-aefd-4ea8-878e-3a845c6cb114`

连续多次任务均失败在意图分析阶段，典型错误：

```text
MODEL_GATEWAY_UNAVAILABLE
模型网关当前不可用，意图分析没有拿到大模型结果。
```

最底层真实错误：

```text
MODEL_PROVIDER_ERROR
The read operation timed out
```

调用链实际停在：

```text
intent-analysis-engine-original
→ foundation-gateway
→ model-dispatcher
→ DeepSeek
```

尚未进入：

- workflow-execution
- knowledge-qa
- knowledge.retrieve

当前配置文件：`framework/config/model.env`

已确认的非敏感配置：

```text
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_TIMEOUT_SECONDS=30
DEEPSEEK_API_KEY：已配置，但不能据此确认 Key 有效
```

本机网络检查结果：

- `api.deepseek.com` 可以解析到 IP；
- TCP `443` 连接失败；
- 因此当前更像是网络、防火墙、代理或外网访问问题；
- 如果模型名或 Key 无效，通常应返回 HTTP 错误，而不是一直读取超时。

意图分析引擎明确设置了：

```text
allow_fallback: false
```

这是因为用户之前要求不使用固定话术或固定意图兜底。因此模型调用超时后，任务直接失败，不能生成意图确认卡，也不会继续知识库问答。

相关文件：

- `framework/layers/foundation/model_dispatcher/service.py`
- `framework/layers/business_engine/intent_analysis/delivered_engine/service.py`
- `framework/config/model.env`

## 七、当前判断

### 已正常

- 知识库上传链路已打通。
- 数字资产引擎已融入整体框架。
- 原文件保存和元数据登记正常。
- Excel 解析和知识片段保存正常。
- 重复文件复用机制正常。
- 知识库索引状态和片段数量已经有真实留痕。

### 当前问题

1. PDF 解析结果不可用，需要修复 PDF 文本解析能力。
2. 模型网关无法连接 DeepSeek，导致意图分析阶段超时。
3. 因为 `allow_fallback=false`，模型不可用时不会继续执行知识库问答。

## 八、下一步建议

### 优先处理模型网关

先确认：

1. 当前电脑是否允许访问 `api.deepseek.com:443`。
2. 是否需要配置系统代理或企业网络代理。
3. `DEEPSEEK_API_KEY` 是否有效且有余额/调用权限。
4. `DEEPSEEK_MODEL` 是否为当前供应商实际支持的模型名称。
5. 重启后端时是否真正加载了 `framework/config/model.env`。

不要先简单增加超时时间；当前 TCP 连接已经失败，增加超时不能解决网络不可达问题。

### 验证知识库问答

模型网关恢复后，在前端提问：

```text
请根据我个人知识库中的《综合联调测试主数据.xlsx》，告诉我项目编号、区域和产品名称。
```

审计中应看到：

```text
intent_analysis
→ knowledge.query 或 knowledge.qa.answer
→ workflow-execution
→ knowledge-qa
→ knowledge.retrieve
→ knowledge_chunks
```

不应再次出现：

```text
document.table.extract
vector.index.upsert
```

## 九、继续处理时的约束

- 不要为了回答问题硬编码项目编号、区域或产品。
- 不要把知识库问答短路到应用层直接读 SQLite。
- 不要绕过数据操作引擎读取业务数据。
- 不要改变个人知识库只绑定账户的权限规则。
- 不要在没有用户确认的情况下清理数据库。
- 不要提交模型 Key、Token或完整 `model.env` 内容。
- 修改后需要重启后端服务才能生效，但当前对话没有启动或重启服务。
